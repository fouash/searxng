# Read the documentation before extending the defaults:
# https://docs.searxng.org/admin/settings/

use_default_settings: true

server:
  secret_key: "ultrasecretkey"
  image_proxy: false
  limiter: false
  public_instance: false
  default_http_headers:
    Access-Control-Allow-Origin: "*"
    Access-Control-Allow-Methods: "GET, OPTIONS"
    Access-Control-Allow-Headers: "Content-Type, X-Requested-With"

search:
  default_lang: "en"
  safe_search: 0
  formats:
    - html
    - json
  languages:
    - "en"
    - "ar"
    - "ar-SA"
    - "all"

outgoing:
  request_timeout: 6.0
  max_request_timeout: 10.0

ui:
  default_locale: "en"
  default_theme: simple

engines:
  # Disable rate-limited engines that block cloud IPs
  - name: brave
    disabled: true
  - name: duckduckgo
    disabled: true
  - name: google cse
    disabled: true
  - name: startpage
    disabled: true
  - name: qwant
    disabled: true

  # Disable cert/SSL engines to focus on web/domain search
  - name: searx
    disabled: true

  # Keep reliable search engines
  - name: bing
    disabled: false
  - name: google
    disabled: false
  - name: yahoo
    disabled: false

  # Disable domain-specific or problematic engines
  - name: wikidata
    disabled: true
  - name: ahmia
    disabled: true
  - name: torch
    disabled: true
  - name: whois
    disabled: true
  - name: crtsh
    disabled: true
